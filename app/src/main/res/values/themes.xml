<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.GDRitzys" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:statusBarColor">@color/primary</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Bottom Navigation Theme -->
        <item name="bottomNavigationStyle">@style/Widget.GDRitzys.BottomNavigationView</item>
    </style>

    <style name="Theme.Ritzys.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar"/>

    <style name="Theme.Ritzys.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light"/>

    <!-- No Action Bar theme -->
    <style name="Theme.Ritzys.NoActionBar" parent="Theme.GDRitzys">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- Bottom Navigation Style -->
    <style name="Widget.GDRitzys.BottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemIconTint">@color/bottom_nav_colors</item>
        <item name="itemTextColor">@color/bottom_nav_colors</item>
    </style>

    <!-- Shape Appearance for rounded corners -->
    <style name="ShapeAppearance.App.CornerRadius8" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">8dp</item>
    </style>
</resources>